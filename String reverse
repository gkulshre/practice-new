#include <stdio.h>
#include <string.h>
int main()
{
    int i,len;
    char s[1000001];
    scanf("%s",s);
    len=strlen(s);
    char str[len+1];
    for(i=0;i<=len;i++){
        str[i]=s[len-i];
    }
    for(i=0;i<=len;i++){
    printf("%c",str[i]);
    }
        
    

    return 0;
}
